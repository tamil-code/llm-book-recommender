# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
venv/
.venv/
env/

# Environment / secrets
.env

# OS
.DS_Store

# Model / data caches (downloaded at runtime)
.cache/

# Note: chroma_db/ is intentionally committed so the Space starts fast.

# Local-only full export (use pyproject.toml / uv.lock instead)
requirements.txt

# Root-level video (committed copy lives at docs/LLM Book Recommender.mp4)
LLM Book Recommender.mp4

# Demo recording tooling (superseded by docs/LLM Book Recommender.mp4)
.demo_deps/
.playwright-browsers/
docs/raw/
docs/DEMO_SCRIPT.md
scripts/make_demo.sh
scripts/postprocess_demo.py
scripts/record_demo.py
